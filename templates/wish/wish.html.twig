{% extends 'base.html.twig' %}
{% block title %}Page d'accueil{% endblock %}
{% block body%}
    <h2>Liste de souhaits</h2>


    <ul>
        {% for item in wishes %}
            <li class="list-item"><a href="{{ path('app_wish_detail', {id : item.id}) }}"><i class="{{ item.class }}"></i>{{ item.title }}</a></li>
        {% endfor %}
        <li class="list-item">+</li>
    </ul>
{% endblock %}
