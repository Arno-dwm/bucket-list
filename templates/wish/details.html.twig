{% extends 'base.html.twig' %}
{% block title %}Page d'accueil{% endblock %}
{% block body%}
<div class="wish">
    <h3>{{ wish.title }}</h3>
    <p class="wish-info">Created by {{ wish.author }} on {{ wish.dateCreated | date('d/m/Y') }}</p>
    <p>{{ wish.description }}</p>
    <img src="{{ asset('images/'~wish.url~'.jpg') }}" alt="" />
</div>
{% endblock %}
